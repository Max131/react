const firstName: string = "John";
const lastName: string = "Doe";
const age: number = 30;

const person: {
  firstName: string;
  lastName: string;
  age: number;
} = {
  firstName,
  lastName,
  age,
};

console.log(person);
