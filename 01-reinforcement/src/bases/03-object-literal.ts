const firstName: string = "John";
const lasName: string = "Doe";
const age: number = 30;

const person: {
  firstName: string;
  lasName: string;
  age: number;
} = {
  firstName,
  lasName,
  age,
};

console.log(person);
